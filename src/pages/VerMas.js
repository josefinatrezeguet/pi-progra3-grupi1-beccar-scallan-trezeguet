import React from "react";
import VerMasComponent from "../components/VerMasComponent/VerMasComponent";

const VerMas = (props) => {
  return (
    <>
      <VerMasComponent match={props.match} />
    </>
  );
};

export default VerMas;
